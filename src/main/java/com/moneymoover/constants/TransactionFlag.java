package com.moneymoover.constants;

/**
 * A public interface used to contain constants for flags used when
 * specifying search parameters in Transaction Command
 */
public interface TransactionFlag {
    String TRANSACTION_DATE_FLAG = "d";
    String TRANSACTION_MONTH_FLAG = "m";
    String TRANSACTION_DESC_FLAG = "desc";
    String TRANSACTION_CURRENCY_FLAG = "c";
}
